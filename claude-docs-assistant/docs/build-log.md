# Build log

Running session summaries per the workflow standing rules.

## Session 1, July 9, 2026

What changed:

- Phase 0 complete. Repo created at C:\Users\khush\OneDrive\Attachments\claude-docs-assistant with git initialized (local only, GitHub deferred by decision). Folder skeleton: /docs, /corpus/raw, /src, /eval, /.claude. All three spec documents converted from docx to markdown and committed (requirements-v2.md, ui-design-spec.md, workflow.md).
- Phase 1 complete. CLAUDE.md project memory written (platform constraints, writing rules, architecture rules including the MOCK_MODE single-boolean rule, process rules). Two skills created: design-guardrails and corpus-chunker. Design spec section 10 QA checklist amended with "mock mode disabled" item per author instruction (spec version history bumped to 1.1).
- Phase 2 research complete. Four parallel research passes (AI answer interfaces, minimal product benchmarks, design galleries, hierarchy-without-color principles) synthesized into docs/ui-research-notes.md (12-pattern summary table plus four open questions) and docs/ui-direction-proposal.md (three directions: A teal instrument, B ink on paper, C monochrome with one signal).

Decisions taken by Khushi this session: project location, defer GitHub, agent fetches corpus pages later, mock mode approved as one CONFIG boolean defaulting to false.

Gate 1 decision (Khushi): Direction C, monochrome with one signal; score bars solid ink-gray (no amber). Phase 2.5 applied to design spec section 5 (version 1.2): 5.1 aesthetic direction rewritten, 5.2 palette rewritten (ink #171717, ink-soft #666666, surface #FAFAFA, line #E4E4E4, single teal accent #0E7C7B on interactive elements only, bar-fill #3D3D3D grayscale bars), "hue only on interactive elements" rule added. Two downstream amber references reconciled (6.5 relevance bar, section 10 craft checklist), flagged in the change note since they sit outside section 5.

RESOLVED accent contrast flag: the spec now defines the palette as Radix Colors scale steps (spec 1.3). Accent link text is Radix teal 11 (#008573) on gray 1 to 2, about 4.6:1, which meets WCAG AA by construction. This supersedes the earlier hand-picked #0E7C7B (about 4:1). The build-log accessibility flag is closed.

Also this turn (Khushi's two design-system decisions): (1) palette adopts Radix Colors, gray neutrals plus teal accent plus red errors, mapped by Radix step conventions (1 to 2 backgrounds, 6 to 8 borders, 9 solid fills, 11 accent text). Artifact inlines the published Radix light hexes into CONFIG since external CSS cannot load. (2) design-guardrails skill now names 21st.dev as an approved reference for component structure and interaction (AI Chat, Input, Empty State, Loader), reference only: extract the pattern, rebuild within artifact constraints, restyle with our tokens, confirm no banned elements survive.

Phase 3 corpus construction done, stopped at Gate 2. Fetched 5 docs pages into /corpus/raw/ (URL as first line): the consolidated "Prompting best practices" page plus four guardrails pages (reduce hallucinations, increase consistency, mitigate jailbreaks, reduce prompt leak). Discovery note: the old granular prompt-engineering pages now redirect into the single best-practices page, so the 8 pe- chunks share that page URL (content is faithfully present); the 4 guardrails pages remain distinct. Authored /corpus/chunks.json: 15 chunks (8 pe-, 7 gr-). Wrote /corpus/validate.mjs and generated /corpus/validation-report.md: all checks clean (no missing fields, no duplicate ids, valid prefixes, keyword counts 3 to 8, token estimates 147 to 254 inside the 150 to 500 target). Keyword pass applied the vocabulary-gap test (beginner synonyms not in the text). Documented coverage and known out-of-corpus boundaries in README.md.

Two design-system decisions from Khushi were applied before Phase 3: Radix Colors palette (spec 1.3) and the 21st.dev reference-only rule in the design-guardrails skill.

Corpus expanded at Khushi's request ("you should only make it", she handed the manual curation back). Removed the 12 blank placeholder files and the author-handoff framing; _FORMAT.md is now a maintenance reference. Added 9 chunks: 7 more prompt-engineering sections from the best-practices page (tool usage, parallel tool calling, long-horizon state tracking, subagent orchestration, avoid over-engineering, frontend design, vision) and a fifth guardrails page, Reducing latency (2 chunks: model choice and TTFT, plus length and streaming). Corpus is now 24 chunks across 6 pages (15 pe-, 9 gr-); validation clean. README coverage and out-of-corpus refusal targets updated (vision and tool use are now in corpus, so dropped from the refusal list).

Gate 2 approved by Khushi ("go ahead with the evals and stuff"). Proceeded through Phase 4 and Phase 5.

Phase 4 (eval set): wrote /eval/eval-set.md from the raw pages: 10 E-1 grounding questions (2 partial-coverage), 5 E-2 refusal + 1 injection, empty results tables for E-1 to E-7 with pass criteria.

Phase 5 (implementation): built /src/app.jsx, the full single-file React artifact, in one pass following the requirements build order (CONFIG, CORPUS, RETRIEVAL, GENERATION, UI) and the design-guardrails skill. Includes the acknowledgment gate as a top-level conditional render (FR-1.4), the 24-chunk corpus inlined (data-only), BM25-lite retrieval with keyword boost and precomputed stats (FR-3), zero-score short-circuit canned reply (FR-3.6), section 6 system prompt assembler, one Messages API call per message with the 4.6 parsing contract, history policy (DD-2, no historical chunks resent), full UI (header readout, empty state pills, message anatomy with mono AI label, retrieval trace with grayscale bars, mono status, input, error card with retry, new session confirm), Radix palette inlined, two-voice typography on system-stack fonts, reduced-motion and focus-visible handling. MOCK_MODE ships false.

Zero-cost verification done this session:
- Retrieval preview /eval/retrieval-test.mjs (Node, no API) mirrors the app's BM25 exactly and runs the 10 E-1 questions: 10/10 retrieve the correct chunk at rank 1. Strong informal E-3 pass.
- Local browser preview with MOCK_MODE forced on (committed at /preview): visually verified the gate, empty state, message anatomy, retrieval trace with grayscale score bars, zero-score refusal (no trace), Enter-to-send (confirmed via a real keydown; the browser tool's synthetic Return did not map to key Enter, a test-harness quirk, not a product issue), and the 380px mobile layout (no horizontal scroll). Static checks: no em dashes, no gradients/purple/emoji/localStorage/form tags, single default export, MOCK_MODE false.

Reviewer note for Khushi: src/app.jsx uses MODEL "claude-sonnet-4-6" exactly as requirements section 14 specifies. If that model string is stale by the time you run it live, it is a one-line CONFIG change; flag it and I will update, but I did not deviate from the spec on my own.

Retrieval fix during preview: Khushi tested the example pill "How do I stop Claude from making things up?" and the retrieval trace correctly exposed that it pulled the wrong chunks (Frontend design instead of Reduce hallucinations), a vocabulary mismatch (risk R-3). Fixed by adding beginner-idiom keywords ("making things up", "false information") to gr-hallucination-basic in both chunks.json and the inlined app.jsx copy; the chunk now ranks #1 for that phrasing. Added eval/retrieval-test.mjs coverage of all four example pills (now 10/10 grounding, 4/4 pills) and eval/corpus-sync.mjs to guard against chunks.json vs app.jsx drift (both files must match; the artifact ships the inlined copy).

Design redirection (author overrides, July 9, 2026): Khushi chose to move off Direction C. Rebuilt src/app.jsx to a Claude.ai-inspired aesthetic (warm paper, serif display, sans body, mono metadata, coral accent with darker coral for AA link text), added a randomized empty-state greeting (overrides HM-4/HM-10), switched to Copilot-style numbered [n] citations with an always-visible Sources list showing only the cited sources, made MOCK_MODE build a realistic extractive answer from the actual doc text, and implemented graceful out-of-scope (RELEVANCE_MIN cutoff: closest-match link plus the matched query words, no answer). Kept the non-affiliation gate line, descriptive name, machinery readout, per-message AI label, WCAG AA, no emoji/exclamation. Overrides recorded authoritatively in CLAUDE.md; design spec bumped to 1.4 with a Direction C history note; requirements FR-3.6a and section 14 (RELEVANCE_MIN, MOCK_MODE, GREETINGS) updated. Verified in the local MOCK preview: Claude-styled gate, randomized greeting, clean numbered answer + cited-only Sources, and the graceful out-of-scope ("How much does the Claude API cost per token?" -> outside the docs + closest "Reducing latency" link + matched: claude, token). corpus-sync and retrieval-test still pass (10/10 grounding, 4/4 pills).

Design note on the score cutoff: RELEVANCE_MIN is 5.0, but lexical scores cannot cleanly separate in-scope from out-of-scope (the injection question scored 6.87, above a valid pill at 5.65). So the cutoff catches the clearly-weak client-side and the model's rule-3 refusal is the authoritative judge live.

Tier 1 built (author request): cached question-and-answer set as type-ahead suggestions, served instantly with no API call. Authored corpus/qa.json (12 pairs, each answer drafted from the corpus with numbered citations to source chunk ids; Khushi verifies accuracy against the site). Inlined as QA_CACHE in src/app.jsx; eval/qa-sync.mjs guards qa.json vs the inlined copy and checks every sourceId resolves and every citation is in range. app.jsx: as the user types (2+ chars), matching cached questions appear above the input labeled "saved"; clicking one adds the question and serves its cached answer with sources, no API call. Tier 2 (typed novel question) still runs the RAG pipeline. Verified in preview: typing "how do i keep" surfaced "How do I keep a chatbot in character?", and clicking it served the cached answer instantly with source [1]. All guards pass (corpus-sync, qa-sync, retrieval 10/10 + 4/4).

Corpus expanded (author request, accuracy first): added a build-with-Claude area (prefix bc-, 11 chunks) from 5 official pages: structured outputs, tool use overview, prompt caching, extended thinking, streaming. Corpus is now 35 chunks across 11 pages; cached Q&A grown to 17. Added scripts/sync-inline.mjs to regenerate the inlined CORPUS and QA_CACHE in app.jsx from the source JSON (prevents drift; replaces hand-editing the inlined copy). validate.mjs and corpus-chunker skill updated for the bc- prefix. All guards pass (validation, corpus-sync, qa-sync, retrieval 10/10 + 4/4).

Accuracy verification pass complete (author asked me to double-check all content against the official website). Three independent audit agents re-fetched the live pages and checked all 35 chunks and 17 cached answers. Results: guardrails 15/15 accurate, build-with-Claude 11/11 chunks + 5/5 answers accurate, prompt engineering 13/15 chunks + 5/6 answers accurate. Fixes applied: (1) pe-role and qa-role dropped the claim "role prompting is the most effective way to use system prompts" (no longer on the page) and pe-role dropped a character/personality sentence that belongs to the Keep Claude in character page; both reworded to grounded content. (2) pe-clarity restored "numbered lists or bullet points" (had narrowed to "numbered steps"). (3) pe-chain-prompts trimmed two sentences not on the cited page, keeping only grounded claims. (4) bc-tool-types added the advisor tool for completeness. (5) Fixed a duplicate qa id: the build-with-Claude tool-use answer was renamed qa-tool-use-flow (it collided with the prompt-engineering qa-tool-use), and eval/qa-sync.mjs now checks for duplicate qa ids. Re-ran all guards: validation clean, corpus-sync and qa-sync pass, retrieval 10/10 + 4/4. No inaccuracies remain from the audit.

Phase 8 packaging done: full README (run instructions, ASCII architecture, two-tier design, design decisions, corpus, evaluation, deployment status, roadmap, repo layout), docs/interview-notes.md (talking points), MIT LICENSE, .gitignore. Repo is push-ready but not yet on GitHub (no gh CLI / no remote in this environment; author to create the repo and push). Deployment answer for the author: v1 is artifact-only and free (paste src/app.jsx into a claude.ai chat, share via the artifact link). Streamlit does not fit (Python, wrong stack). Vercel is the v2 path but costs money (author-owned API key, needs serverless key holder + rate limit + spend cap). The only zero-cost public option is a bring-your-own-key variant on a static host, which is a v2 change.

Next steps: Phase 6 formal evaluation needs a live run (paste src/app.jsx into a claude.ai artifact and work through /eval/eval-set.md for E-1, E-2, E-4 to E-7; E-3 already previewed). Then Phase 7 anti-generic design QA, Phase 8 ship and portfolio packaging. Gate 3 (Khushi declares v1 done) after the section 13 pass bars are met.

Verification note for Phase 1 exit criteria: start a fresh Claude Code session in the project folder and ask "what are this project's design constraints?" It should answer from CLAUDE.md unprompted.
